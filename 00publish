# checkout to public, and add master's current tree
git my trivial -- -B public --use master public
git checkout public && aurpublish pkgs

# see currently released pkgbuilds:
g my topic infos -r

# new release: update package build
makepkg -g
